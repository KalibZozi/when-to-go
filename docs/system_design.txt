Felület tervek
--------------

bejelentkezés
    username / email, password, (deviceId)

térkép komponens
    https://leafletjs.com

deviceId-k kezelése
    mely megállók, mely járatai, melyik sorban

képernyők
    bejelentkezés
        elfelejtett jelszó
    jelszó csere
    devices
        képernyő beállítás
            új megálló hozzáadása
                megálló, járat, sor kiválasztás
        új device hozzáadása
        meglévő device törlése

táblák
    users
        id
        username / email
        password
    devices
        id
        user_id
    displayed_lines
        id
        active
        device_id
        line
        stop_id
        route_id

CREATE TABLE `when-to-go`.`users` ( `id` INT NOT NULL AUTO_INCREMENT , `username` VARCHAR(50) NOT NULL , `password` VARCHAR(255) NOT NULL , PRIMARY KEY (`id`), UNIQUE (`username`)) ENGINE = MyISAM;




